# AirVeil validation status

## Current work — 0.15.0 build 30

Build 30 is installed with full regression and package verification passing. It addresses the confirmed seated false-lock path with foreground-region quality checks and brightness restoration followed by fresh evidence before departure decisions. The physical retest is in progress; no claim that heuristic camera visibility guarantees detection. See [current checklist](GOAL-CHECKLIST-0.15.0-30.md). × was confirmed to dismiss presentation while monitoring continues.

## Build 29 session recovery

Build 29 is installed and verified after a synchronized probe found a stale internal lock flag while macOS was unlocked. Lock hints still pause immediately; stable valid session evidence now corrects stale state through the existing brightness recovery barrier. Full regression passes, including 415 lifecycle checks. See the [current goal checklist](GOAL-CHECKLIST-0.15.0-29.md) for evidence and remaining physical acceptance.

## Build 28 controls and guidance

Build 28 is installed with separate dim/lock controls, a dismiss-only wear reminder, and a low-light explanation followed by one-time brightness restoration and continued seat monitoring. Full regression and native UI checks pass; new physical low-light/dismissal acceptance remains pending. See [current goal checklist](GOAL-CHECKLIST-0.15.0-28.md).

See [Settings, wear prompt, and wake recovery](SETTINGS-WEAR-AND-WAKE-0.15.0-24.md) for automated evidence and installed builds 24–27. The off action, Settings wait/alignment/automatic-blur flow, and moving live dial passed their recorded physical tests. Build 26's unlock-before-return brightness failure is retained. Build 27's full regression suite and installation checks pass, with source at `66e4d6c`. **Four wearer-confirmed brightness cases pass: both AirPods return orders after manual lock and after actual Apple-menu Sleep.** The sleep traces preserve the 75% baseline; one restores despite a 0.2072725296 wake reading outside the former ownership tolerance. Its later new dim is restored by uncertainty cleanup before motion returns, not by rewear. Automatic departure sleep remains unresolved in low light: that attempt never locked, the wearer cannot add room light, and build 28 now implements the chosen one-time brightness restoration with continued monitoring; the new physical test remains pending. The four brightness passes do not claim automatic departure coverage. Entries below are historical checkpoints; later changes supersede older descriptions.

## Notch recenter coach, 0.8.0 (build 9)

Implemented a native notch-attached camera coach and quiet menu-bar startup. Manual camera setup and automatic recovery drive the same panel: circular mirrored preview, live alignment rail, directional/red guidance, measured low-light messages, paired-evidence progress, and short green confirmation. Hover controls provide Set center, Enable/Pause and settings; the menu provides an explicit controls action and labeled camera-free animation preview. The physical notch is excluded from content; unnotched screens use a floating fallback. Existing camera identity/calibration, bounded checks and AirPods removal/wake policy are retained.

Before testing was deferred, the complete existing test suite passed with the initial coach integration. Subsequent focused runs passed 70 coordinator checks, 22 guide checks, 18 screen geometry checks and 16 native controller lifecycle checks; nine SwiftUI states were rendered at 2x and inspected. Independent review found retry-intent and sleep/preview cancellation defects, which were corrected and covered. Final source builds and signs with the existing identity.

Installed runtime diagnostics confirmed settings hidden at launch, no camera while waiting for AirPods, and the app available to ScreenCaptureKit for self-exclusion without opening Settings. The native controller checks cover nonactivating panel flags and actual attached-screen positioning. These checks do not establish physical calibration or comprehensive fullscreen/hover behavior.

On 13 September 2026, the wearer explicitly asked to finish implementation and leave all further testing for later. Physical camera/AirPods recenter, removal and wake, actual hover/click behavior across Spaces, and subjective animation acceptance are deferred accordingly. Existing saved preferences and center were retained; no new wearer-driven calibration result is claimed. See [notch research](../research/NOTCH-RESEARCH.md) and [recording analysis](../research/NOTCH-MOTION-STUDY.md).

## Verified

- Four cited research reports completed and reviewed before app implementation.
- Native macOS app builds against macOS 26.5 SDK with deployment target 14.0.
- App installed at `/Applications/AirVeil.app`, signature verified, and launched.
- Build 7: 2,094 deterministic synthetic motion/math assertions, 62 motion-delivery lifecycle assertions, 33 real-MotionService reference lifecycle assertions, 99 real-AppModel lifecycle assertions, 22 removal-policy assertions, 24 injected display-sleep service assertions, and 690,785 input-region assertions pass. Device, capture, preferences, and display-sleep APIs are stubbed in lifecycle tests.
- Actual GPU render tests pass at 1x and 2x for transparent neutral pixels, opposite-side masks, monotonic/mirrored feather, premultiplied alpha, opaque source independence, full shield, and top-left image orientation.
- Running native settings window inspected visually; simulated left turn produces right-side blur with a clear left side.
- Independent reviews found and fixed run-loop freshness, terminal retry, buffered-sample lag, display-change reveal, preview failure-state, and shortcut-advertising issues.
- Private GitHub repository created using GitHub Desktop and research milestones pushed.

## Live evidence and remaining acceptance

The first integrated build received AirPods Pro 3 motion at approximately 49–52 samples/s from the left bud. The wearer confirmed that physical head motion blurred the in-app preview. The running app then reported successful live desktop capture and calibrated tracking. The Pause button was exercised and returned the capture status to paused. These are real runtime observations, separate from synthetic tests.

An earlier update added whole-screen coverage, reset defaults, clearer calibration, and idle rendering improvements. Whole-screen preview and reset to 8° onset / 32° full angle / 32 pt blur / 12% feather / 70 ms response were verified through the running UI. Paused static app CPU was observed near 1.1% after optimization, compared with a prior snapshot near 38%; these snapshots are not a controlled energy benchmark.

Synthetic native-resolution GPU benchmark at the main display's 1920×1080, 1x mode: 120 changed frames, last observed median 0.773 ms and p95 2.995 ms. This measures blit + three Gaussian passes + composition, not real display FPS or sensor latency. Eight repeated renderer release/reuse cycles pass; late frames are rejected. See Tests/PerformanceTests.swift to reproduce.

Persistent permission across a changed signed build, live desktop capture, wearer-confirmed left/right whole-screen sweep, fullscreen coverage on two displays, and the physical global shortcut pass. The wearer subsequently confirmed build 4 blur blocks clicks and scrolling; its diagnostics recorded 88 blocked pointer events. Build 5 removal-triggered display off and normal wake are wearer-confirmed below. Extended calibration, selected-display-only capture, and full system sleep/reconfiguration remain additional physical coverage beyond these accepted workflows. No broader hardware-completion claim is made.

The oldest startup sample's absolute acquisition age remains unverified because headphone timestamp-to-host epoch was not assumed. Increasing delivery lag is detected relative to the best observed offset within a source session.

No iPhone/iPad system-wide version is claimed. No confidentiality guarantee is claimed for blur.

## Persistent identity and automatic tracking update

The prior ad hoc builds changed identity on rebuild. AirVeil now uses a persistent, self-signed local development certificate in an isolated user keychain outside the repository. No global root trust was added. Strict signature validation passes, and the default designated requirement pins the bundle identifier and leaf certificate, rather than the executable hash. The keychain must temporarily be in the search list during signing; the helper restores the prior list and locks the signing keychain afterward.

Only AirVeil's stale ScreenCapture approval was reset with the supported `tccutil reset ScreenCapture com.shivhax.airveil` operation. No permission database was edited directly. The user approved the replacement identity, and an actual ScreenCaptureKit shareable-content request succeeded. Build 0.2.0 (2) was then installed without resetting permission. Its code hash differs from the approved build, its designated requirement is identical, it satisfies the old requirement, and the launched update reports screen access allowed. The updated build subsequently started live capture successfully with no new approval. Runtime state reported build 2, captureRunning/captureReady true, no capture error, and screenPermission true. The wearer confirmed that actual desktop blur sweeps correctly in both directions.

Automatic tracking is now started in normal application startup, independently of diagnostics. The installed app found the worn AirPods without a Connect button and received approximately 47–50 samples/s. Connection callbacks and timed retries recover motion automatically; center remains an explicit user choice. Wake detection restarts the sensor, while the desktop effect stays paused until recalibrated and enabled. Physical reconnect/wake acceptance remains pending.

Whole-screen mode now uses a directional sweeping edge instead of equal blur across both halves. Updated actual-GPU tests pass at 1x and 2x for quarter, half, and three-quarter sweep positions, mirrored direction, monotonic coverage, exact clear/full endpoints, and smooth reversal. The same mode is forwarded to both the app preview and desktop overlays.

Identity update evidence: approved build code hash `c379ab4f7bd2f8139ca5d17f875f31d4a723aea5`; updated build code hash `5c475b4edfb66470bdc717a56f234b9146b73f37`. Both use the same certificate-bound designated requirement. Private signing credentials are outside the repository and were not included in this evidence.

During live testing, a transient delayed-motion event invalidated calibration and activated the documented protective cover. Fresh samples recovered automatically; a subsequent Set center restored tracking. The final inspected UI showed Following your head, Live desktop capture, and Whole-screen sweep enabled. This observation does not establish sustained calibration reliability.

## Reliability update 0.2.1 (build 3)

- Root cause of UI-induced delayed-motion warnings: acquisition callbacks were on the main queue and their receipt timestamps included UI scheduling delay. Collection now runs on a serial background queue. A bounded buffer delivers the newest pose while retaining intervening sensor gaps, source changes, invalid data, clock changes, and terminal errors. The watchdog consumes queued acquisition before checking freshness. Synthetic tests cover a one-second UI stall, menu-tracking coalescing, real sensor lag, gaps, source switches, reference jumps, invalid samples, terminal errors, and expired acquisition.
- Fixed a queued-start cancellation race: immediate Pause or shutdown before the async capture/access-check task begins now prevents that task from starting. The real AppModel tests fail when either entry guard is removed.
- Capture startup now rechecks display identity, exact frame, backing scale, generation, and failure state across each async boundary. This prevents starting a partially outdated display set or continuing after an early stream failure.
- Additional actual-GPU tests pass for newest captured-buffer selection, unchanged idle frames, 16 alternating live source images without retained trails, clear-side transparency, and resized source allocation.
- Build 3 installed and its actual ScreenCaptureKit access check passed without new permission. The running UI observed automatic AirPods motion at about 51 samples/s after reconnection and manual center selection.
- Main display inventory at test time: 1920×1080 at 1× and a second display at (1920, -239), 1470×956 points at 2×. Current live two-display alignment, fullscreen coverage, physical reconnection, and manual pause-key checks are requested from the wearer.
- Automated key injection from GitHub Desktop did not increment the app's new global-pause activation counter. This does not establish whether a physical hotkey is delivered; manual confirmation is pending. Registration itself succeeds.

All current automated suites and a complete signed build pass. The latest physical checks remain separate from this result.

A 90.2-second live observation of build 3 collected 178 fresh diagnostic snapshots: capture stayed ready in all 178; motion was fresh in 170 and calibrated in 148. Maximum added acquisition lag was 48.7 ms, with no delayed-motion warning in that interval. Eight snapshots were waiting for motion and 22 reported a head-reference jump; 30 snapshots used the protective cover. The wearer was carrying out requested physical checks, so this aggregate does not attribute the interruptions to a specific gesture or prove sustained calibration. Manual feedback is still pending. No head-pose history or screen image was saved in this aggregate.

## Wearer feedback and update 0.3.0 (build 4)

The wearer confirmed fullscreen blur on both connected displays and confirmed that the physical Control–Option–Command–P shortcut immediately clears it. Diagnostics independently recorded one global-pause activation and stopped capture. The wearer also reported a steady ten-second turn followed by a black screen that remained until Set center after reinserting a bud. This was not accepted as satisfactory recovery.

The new recovery policy automatically pauses and clears blur and pointer blockers when tracking becomes invalid. Fresh input alone does not invent a new reference; Set center resumes a pending interrupted effect, while manual Pause or changing display selection cancels that recovery intent. A changed reference therefore no longer leaves a persistent black cover.

The discontinuity detector now considers both adjacent angular velocities so an abrupt physical stop is not assessed using only a low final speed. Actual unexplained low-speed quaternion jumps still invalidate calibration. New referenceJumpCount/lastReferenceJump diagnostics record one event count plus the latest step/threshold/timing/rates, without a pose history. A sensor-origin reset remains unproven until live event evidence identifies it.

User-requested controls now include connected-display count, refresh, per-display selection persisted by UUID, and click/scroll blocking in blurred areas or the whole affected display. Nonactivating panels preserve keyboard focus and reserve menu/escape controls. Drags already started before interception may remain owned by the original application.

Automated validation passes: 2,094 math assertions, 52 motion-delivery assertions, 42 real-AppModel lifecycle assertions with entirely stubbed device/capture/preferences APIs, 690,785 input-region checks, actual-GPU input-mask cross-checks at 1×/2×, and prior live-texture replacement/resize tests. The new selected-display, pointer-interception, and automatic-clear behavior still need installed-app verification.

Build 4 is installed and strict signature validation passes with the existing identity. Actual ScreenCaptureKit access check passed without new permission. The running controls were inspected visually; selecting displays updated 2 → 1 → 0 → 1 → 2, and both were restored. The installed app currently reports two connected/two selected displays and pointer blocking enabled in Blurred area mode. AirPods motion is unavailable at this final setup check, so live selected-display capture, physical pointer interception, and new recovery behavior await the wearer's requested checks.

## Removal display off, 0.4.0 (build 5)

The wearer confirmed that blur works and clicks/scrolling are blocked. Build 4 diagnostics independently recorded 88 blocked pointer events. A temporary synthetic pointer fixture was closed without running its blocker because physical acceptance supplied the requested evidence.

The new optional removal action uses explicit Core Motion disconnect events, armed only by live motion. A 1.5-second debounce allows reconnect or earbud handoff; new disconnect events restart that delay. Stale motion, reference jumps, and startup without worn headphones do not trigger display sleep. The action works independently of blur, is one-shot until fresh motion rearms it, and does not automatically unlock the Mac. Reset defaults disables it. It uses the documented `pmset displaysleepnow` command; all tests use injected runners and never execute that power action.

All automated suites pass, including 56 actual-AppModel assertions, 22 removal-policy assertions, and 24 injected command-service assertions. Build 5 is installed, strict signing verification passes, and the installed designated requirement matches build 4. Actual ScreenCaptureKit access check passed without renewed approval. The new controls were visually inspected and automatic display off was enabled for the wearer.

The Lock Screen settings button opened the correct page. Read-only inspection showed Require password is already Immediately, and Automatic Ear Detection is already on. No Mac power or authentication settings were changed. Actual both-earbud removal, physical display-off, normal wake/unlock, and lack of repeated sleeping remain pending the wearer check. At install inspection only the built-in display was connected; no current two-display removal claim is made.

The wearer then completed the requested removal test and reported that blur still works, removing the AirPods turns the display off within two seconds, and pressing a key wakes it again. Runtime diagnostics recorded one display-sleep request for the disconnect, followed by resumed fresh headphone motion and Ready status after return, with no additional sleep request in that check. Two displays were connected during the runtime observation. This establishes the requested removal/display-off/wake workflow; it does not separately establish the authentication prompt or an exact per-display visual observation. The Mac's already-Immediate password setting was observed separately above.

## Superseded automatic center, 0.5.0 (build 6)

The wearer requested centering without repeated button presses after taking the AirPods off. Automatic center is now on by default and retains the existing blur/display preferences. It calibrates once from fresh steady motion after a new wear/start session, explicitly assuming the wearer briefly faces the screen. Same-connection reference changes and ordinary held turns cannot silently become a new center. Manual Set center remains available.

The coordinator preserves prior active-effect intent through removal and sleep, but manual Pause, display selection/reset, or disabling automatic centering cancels pending restoration. Separate system/display/session state prevents an observed inactive session from automatically restarting capture. The automatic-center counter records successful automatic calibrations without pose history.

All automated suites pass, including 97 AppModel lifecycle assertions covering steady-pose timing, held-turn preservation, same-session reference changes, reconnect restoration, manual cancellation, removal-triggered display sleep, and session changes before/during asynchronous capture startup. Complete signed build 6 is installed with the existing identity; new controls were inspected visually, Automatic center is on, and the earlier actual access check on build 6 succeeded without renewed approval. A final copy-only wording refinement retains the same signing requirement. The wearer's no-button removal/reinsertion and automatic-resume check is pending.

Subsequent live observations recorded four automatic calibrations across disconnect/reconnect episodes, with one display-off request and fresh motion on return. The effect remained manually paused during those calibrations. After Enable was selected, a separate 45-second observation ended with capture ready on the one selected display, two displays connected, and fresh calibrated motion; it contained no further disconnect, so it did not establish automatic capture resumption. The wearer confirmed that automatic centering ran, but reported that rewear while facing elsewhere chose that new direction as zero. This behavior does not satisfy remembering the original screen-facing center; build 7 replaces it.

## Retain original center, 0.6.0 (build 7)

Removal and display/session suspension now retain the original reference, manager, and outstanding motion stream. Missing samples pause capture and input blockers. Holding still never recalibrates; only an explicit Set center replaces zero. A fresh return may resume previously active capture using the retained reference. Manual pause, selection changes, and reset cancel that resumption. Detected source, clock, or reference changes invalidate use of the stored reference rather than replacing it silently.

Synthetic sensor tests exercise the real MotionService through injected transport, clock, and attitude boundaries. They establish that the application can retain its chosen value through a long absence and rewear at a different angle. They do not establish that Apple's physical sensor frame remains unchanged. The away-facing rewear and return-to-original-zero hardware check remains required after installation. App relaunch creates a new motion session and requires an initial Set center.

All automated suites pass, including actual GPU tests at 1× and 2×. Source-clock reversal and an offset change after explicit absence recover fresh samples only after multiple advancing samples; the old reference remains invalid until explicit Set center. Isolated old packets, buffered bursts, and ordinary delivery lag cannot reset the timing baseline. The complete signed build 7 was installed with the identical designated requirement and strict signature verification. Its actual ScreenCaptureKit access check passed without renewed approval. The running controls were inspected visually and the app received approximately 50–51 samples/s from the left AirPod. Existing whole-screen, input-blocking, removal-display-off, and one-of-two display selections were retained. The controlled original-zero hardware test has been requested; its outcome is pending.

A subsequent 40-second live observation inspected 80 fresh snapshots from the installed diagnostic process. Center revision remained 2 through a disconnect/return event: the saved state changed to awaitingReturn, capture paused, the display-off count advanced once, motion returned with retainedAfterGap, and capture resumed. The final snapshot had fresh calibrated motion and live capture ready with the same left-bud source. No pose history was stored. The public heading and magnetic-accuracy fields both reported -1; this observation supplies no absolute-heading anchor. Unchanged center revision and resumed capture prove application retention/recovery in that event, not physical alignment with the screen; the wearer's original-direction check remains pending.

A later live event exposed a remaining interrupted-tracking case. After further disconnects, the active sensor was the right AirPod and the saved reference was invalid. The recorded same-source attitude step was 32.0° across 20 ms, above the 20.1° threshold, with bounding rotation speeds of 8.0 and 17.4°/s and about 23 ms added delivery lag. This establishes a measured discontinuity, not its cause. Merely changing source is a separate conservative invalidation rule; the source-change pair itself skips the jump comparison. A following 45-second observation saw an explicit Set center advance revision 4 to 5 and restore fresh tracking and ready capture. Physical screen alignment after an unchanged reference remains unconfirmed, so retained-center hardware acceptance is incomplete.


## Camera anchor and energy update, 0.7.0 (build 8)

The wearer explicitly rejected build 7's retained-reference behavior: removing AirPods while facing left and reinserting them shifted the apparent screen center, including when reinserting while facing right. Keeping the same saved quaternion and unchanged center revision did not preserve physical heading. This supersedes the pending physical acceptance above. AirPods-only mode now makes a saved reference unusable after missing motion/removal and requires explicit Set center; it does not guess zero from a newly still pose.

Three additional reports investigate independent camera anchoring, per-session AirPods/camera alignment, and energy use: [camera research](../research/CAMERA-ANCHOR-RESEARCH.md), [fusion design](../research/HEADING-FUSION-DESIGN.md), and [energy research](../research/ENERGY-RESEARCH.md). The implemented camera prototype is optional and off by default. Its Enable button is the only camera permission-request entry point. Built-in-camera frames are analyzed locally and discarded. A screen-facing setup plus a short head turn saves camera-space neutral and learns yaw sign. Brief recovery bursts measure the current turned angle against that durable center, rebuilding only the disposable AirPods alignment. A missing/ambiguous face, changed layout, or failed check cannot silently establish a new zero. Physical camera correctness remains unverified.

The energy complaint was independently observed: Activity Monitor showed current Energy Impact 1556.2, while a paused, noncapturing AirVeil process used 36.1% CPU in a snapshot and averaged 53.76% over a subsequent ten-second observation. The profile contained substantial SwiftUI view reconstruction. These observations were not a controlled active-blur comparison; Energy Impact is a relative metric, not watts. The old app was stopped during development to avoid continued load.

The first optimization stage isolates rounded tracking telemetry in small views, sends animation directly to the renderer, pauses a settled animation clock, stops hidden/inactive preview work, and deduplicates menu/window changes. Rendering is demand-driven with bounded/coalesced frame notifications and GPU-busy retry. Clear or fully solid overlays skip source blits and all Gaussian passes; returning to blur consumes the newest retained frame. Native capture size, 60-fps ceiling, and the existing blur-quality bank are unchanged. Live energy savings still require an installed-app comparison.

The integrated automated run passed 2,094 math, 62 motion-delivery, 37 real MotionService reference-lifecycle, 36 fusion, 14 camera-service, 32 real camera-coordinator, 140 real AppModel, 22 removal-policy, 24 display-sleep, and 690,785 pointer-geometry assertions/checks. Device, camera, power commands, permissions, and preferences are injected in lifecycle suites. Actual GPU tests passed at 1x/2x, including demand coalescing, no blur for clear/solid output, first-frame completion, newest-source reveal, hidden suspension, cached blur, bounded in-flight retry, and stale-generation release. Eight resource release/reuse cycles passed. The 1920x1080 synthetic 120-frame run measured median 0.755 ms and p95 3.465 ms GPU work, not live power or end-to-end latency. Review then identified and corrected fused-tracking button enablement, unobserved refresh freshness, repeated mismatch notifications, common-mode motion wakeups, and inactive-session preview work; targeted regression results follow below.


Post-review targeted suites pass: **35 real camera-coordinator assertions** (including repeated display-mismatch publication deduplication) and **157 real AppModel assertions** (including inactive system/display/session suspension, visible-preview resumption, and camera-valid UI/activation despite invalid legacy calibration). A standalone in-process run-loop probe confirmed that RunLoop.main delivery stalls during event tracking; the production wake path now uses asynchronous main-dispatch delivery while the display link remains in common modes.

Build 8 was then installed at `/Applications/AirVeil.app`. Its strict signature verification passed and its designated requirement exactly matched installed build 7. The actual ScreenCaptureKit access check passed without renewed permission. UI inspection confirmed camera assistance off, two connected/one selected display, existing whole-screen/input/removal preferences, and accessible camera opt-in and Set center controls. Camera activation has not been performed by automation; the wearer has been asked to opt in and run setup.

A ten-second paused installed-build observation averaged 1.94% CPU (range 0.3–16.0%), with no active capture or camera. No fresh AirPods motion was available in that interval. Because the earlier high-CPU baseline had fresh motion, this is **not a matched savings result**. Active camera setup, removal while left/reinsertion while right, return to original screen zero, and matched live-motion/active-blur energy measurements remain pending.

## Notch tracking repair, 0.8.1 (build 10)

The wearer rejected build 9's notch calibration. The supplied 73.7-second recording contains three setup timeouts and repeated near-complete hold resets. The curved indicator sometimes follows image position, then returns to its middle while the head remains turned. The [recording analysis](../research/NOTCH-FAILURE-RECORDING.md) and [primary-source tracking investigation](../research/NOTCH-TRACKING-REPAIR.md) distinguish observed behavior from source-level causes. Original recordings and face crops are not committed.

The repair removes the new thumbnail/frontal-pose presentation gate from calibration, restoring the earlier fusion acceptance rules. Pending camera evidence now waits for actual post-capture AirPods samples rather than elapsed UI time alone. An unpairable sample does not erase unrelated valid pairs; existing freshness, consistency, and full stationary-window checks still control acceptance. A deliberate Set center reuses an already learned camera sign when the camera/configuration match, even after a changed display layout, while measuring a new neutral and requiring separate fresh recovery. Changed camera/configuration still requires the observed turn. Automatic recovery cannot replace the stored center.

The curved indicator now observes a separate high-frequency AirPods pose model. Valid paired camera yaw anchors its visual reference and fresh AirPods deltas update it between 3 Hz camera observations. Before anchoring, its caption explicitly identifies relative head movement. Face loss retains relative motion rather than snapping the marker to zero; missing motion hides the marker. This display estimate cannot approve calibration or drive blur. Thumbnail delivery coalesces the newest image into at most one pending UI notification, with Vision analysis prioritized ahead of optional preview conversion.

The complete automated suite passes: 2,094 motion/math, 62 delivery, 37 motion-reference, 36 fusion, 35 camera-service, 107 coordinator, 22 coach-state, 30 live-indicator, 157 AppModel, 22 removal-policy, 24 display-sleep, and 690,785 input-geometry checks; actual GPU rendering checks at 1x and 2x also pass. Native notch checks pass 18 geometry and 16 controller lifecycle cases; nine 2x states render. These use injected device streams and do not establish physical sensor accuracy. The timing regression was reproduced against build 9's coordinator; the repaired implementation passes it. Independent read-only reviews found no actionable defects in the repair or the layout/sign follow-up.

The signed build is installed at `/Applications/AirVeil.app`, with strict verification and the same designated signing requirement as build 9. The earlier app is retained in the ignored build directory. Runtime inspection confirms build 10, hidden settings at startup, camera assistance retained, desktop effect paused, one connected display, and fresh Right AirPod motion at approximately 50 samples/s. A changed display layout still correctly requires deliberate Set center. The wearer has been asked to try the repaired check. Calibration time, physical direction, and camera/AirPods accuracy in this repaired build remain pending that test; no aesthetic fluidity or density revision is claimed.

The wearer subsequently tried build 10 live. UI and diagnostics confirmed a successful check with restored valid tracking and the camera stopped. A following 45-second observation captured three further complete setup/recovery successes in approximately 6.0, 4.5, and 5.5 seconds (0.5-second diagnostic sampling limits timing precision). All 91 snapshots in that interval had fresh AirPods motion. The live indicator changed during the checks and reported both AirPods-only and paired camera/AirPods sources. An additional 40-second observation contained 11 paired-camera feedback snapshots and changing indicator angles. Only state transitions and aggregate ranges were inspected; no pose history or new face images were saved.

Later attempts also timed out after face/pose warnings, and a final attempt was canceled. These outcomes remain part of the result: the successful runs establish working live acquisition in build 10, not universal reliability or physical angular accuracy. Wearer feedback on perceived response and direction is still pending. The observation also exposed stale settings text after accepted frames; a follow-up corrects status consistency and preserves the current obstacle in timeout guidance without changing acceptance or timeout policy.

The status follow-up passes 117 coordinator checks, including rejected-to-accepted status recovery and face/motion timeout explanations. It is packaged as 0.8.1 build 11. It changes presentation consistency only; the build 10 live observations above apply to the unchanged acquisition implementation.

## One-pass facing-center check, 0.9.0 (build 12)

The wearer clarified the desired behavior after trying 0.8.1: look straight ahead within approximately 5°, complete one quick check, and never accept a 13–15° turn as a new center or restart a second scan. This supersedes the previous arbitrary-neutral setup and off-axis recovery design. The old two-stage behavior is not an accepted result.

The production coordinator now requires absolute Vision yaw within ±5° for both setup and recovery. Three distinct centered camera observations, spanning at least 0.6 seconds at the existing 3 Hz analysis rate, must match a continuous stationary AirPods interval extending 0.6 seconds before the first capture and 0.2 seconds after the last. The same batch atomically establishes the facing-camera reference and active sensor offset. It preserves the valid motion history, stops capture once, and presents success without a second start. Setup stores an explicit facing-camera mode with zero camera neutral; legacy arbitrary-angle records require deliberate Set center before use. Recovery preserves the saved record and changes only the disposable sensor alignment. The accepted ±5° pose defines runtime zero, so physical zero can differ within that tolerance; no camera yaw sign is fabricated for calibration.

The notch shows deviation from absolute camera-forward, not from the angle at check startup or an old saved neutral. A matched previously observed camera sign enables AirPods interpolation between scans. Without one, the UI honestly shows unsigned camera deviation. No face or motion is represented as a fabricated centered marker. Settings, the demo, guidance, and timeout explanations describe the new single-pass workflow.

**Open Edge Light** is available during a supported active camera check. This Mac's hardware, OS, and active camera format support Apple's Edge Light. The button uses Apple's public Video Effects UI; the wearer chooses Edge Light there. It does not toggle a setting, extend capture, request permission, or establish alignment. [Research and API evidence](../research/EDGE-LIGHT-CENTER-CHECK.md) describe the supported boundary. The button layout was rendered using an injected camera; its real system-panel interaction is not yet observed.

Full automated validation passes: 2,094 motion/math, 62 delivery, 37 motion-reference, 55 fusion, 43 camera-service, 69 centered coordinator, 42 coach-state, 31 absolute-center indicator, 157 AppModel, 22 removal-policy, 24 display-sleep, and 690,785 input-geometry checks, plus the existing actual GPU tests at 1x/2x. Eighteen notch geometry and sixteen native controller lifecycle checks pass; ten 2x states render, including Edge Light controls. Centered coordinator tests cover ±13/15° rejection, ±5° boundaries, continuous 3 FPS completion within two seconds, one capture start, no second check, old-center migration, wrong camera/configuration/layout, motion gaps, actual future sensor receipts, cancellation, and subsequent head rotation staying nonzero. Independent read-only review found no actionable defects.

Build 12 was signed and installed at `/Applications/AirVeil.app`; strict signature verification passed and the previous app was retained locally. Live UI initially required replacement of the legacy center. The wearer's first new check rejected an estimated 6° pose, then completed in approximately 2.5 seconds from observed camera start with one start/stop and valid tracking. The next attempt rejected turned poses and completed after returning to center in approximately 8.5 seconds overall; it did not restart a second scan. These times have approximately 0.5-second diagnostic sampling precision. Runtime confirms build 12, facing-center setup stored, camera stopped after success, and fresh AirPods near 50 samples/s. Physical angle accuracy, repeated reliability, and the wearer's acceptance of the new behavior remain separate from these observed successful runs.


## Approved checkpoint and seated dimming, 0.10.0 (build 13)

The wearer explicitly accepted 0.9.0 with “Everything is perfect!” before requesting this feature. The approved commit is `b2adc0ccd6ad47cda00cc39ac2d4c58f85ac6071`, already saved on `origin/codex/notch-coach`; the named GitHub checkpoint `origin/codex/stable-v0.9.0-centered-check` was also pushed and verified at that exact commit. New work is isolated on `codex/presence-aware-removal`.

The new removal policy uses the existing actual-disconnect debounce. Optional seated presence uses local upper-body and face geometry from the last successful centered check, rejects smaller/background/side candidates, accepts a profile/back body without requiring a face, and latches confirmed departure. Ambiguous/stale/low-light evidence is unknown with a bounded eight-second grace, not invented presence. No seat reference falls back to the existing display-sleep action. Geometry is not identity and cannot distinguish an unseen same-seat replacement.

Brightness uses the actual built-in display control, with an 8% default and 5–50% slider. The original level is captured once, journaled through adjustments, and restored on rewear/cancellation/active quit. Manual changes win. A public idle-display assertion is held during valid seated dimming; manual lock/sleep stops presence and defers brightness writes until an active session. Camera teardown and brightness restoration must complete before the heading camera can restart.

An isolated real-hardware smoke run read original brightness `0.81249994`, applied `0.07999999` and `0.1`, then restored exactly `0.81249994`. All operations reported success; the restore journal was empty and the idle assertion released afterward. This tested the production dimming service against the physical built-in display, without camera use, lock, or display sleep. Full-app wearer presence/profile/background acceptance is separate and pending.


Final automated suites pass: 2,094 math, 62 delivery, 37 motion-reference, 55 fusion, 49 camera-service, 81 centered coordinator, 42 coach-state, 31 notch-feedback, 237 real AppModel/removal integration, 33 anonymous geometry, 14 presence capture lifecycle, 95 dimming, 42 removal-presence coordinator, 22 removal debounce, 24 injected display-sleep, and 690,785 input-geometry checks. Actual Metal rendering passed at 1x/2x; 18 notch screen geometry and 16 native lifecycle checks pass, and ten 2x states render. Test camera/motion/brightness/power boundaries are injected; these counts do not claim physical presence reliability. Independent review fixes cover late restoration retry, rapid second removal during cleanup, face-only continuity after frame gaps, inactive session launch, manual lock during async quit, and manual brightness overrides retaining seated idle protection. Final review found no remaining concrete P1/P2 issues.

Build 13 was signed and installed with exactly the same designated requirement as the approved build 12, which is also retained as an ignored local app backup. Installed and built executable SHA-256 match: `e2e3735bff4a663351dcf62a8037b009994eba26b9849f1f20f9e7a1989586d0`. Normal application launch confirms build 13, settings hidden at launch, master removal and seated dimming enabled, brightness target 8%, presence idle/camera off, no brightness restore pending and no idle assertion held. Existing automatic centered recovery completed after launch, yielding a usable temporary seat reference and fresh motion; no physical removal test was initiated.

The wearer explicitly chose “Finish the update; I’ll try it later.” Physical removal while seated/turned away, background-occupant exclusion, leaving/locking, and subsequent reinsertion remain deferred. The implementation and real brightness control are verified separately from those deferred wearer checks.

## Zero brightness, per-bud removal and notch redesign, 0.11.0 (build 14)

The wearer accepted the 0.10.0 behavior for removal/departure, and requested 0% seated brightness, restoration after either-bud reinsertion, independent left/right onset controls, default-off direct Face light, and reference-driven notch motion. The accepted source remains at `aa83c82e3c400807b4acb072319c7b68b91fb9b1` on `origin/codex/presence-aware-removal`; a local `codex/stable-v0.10.0-presence` checkpoint also retains it. Current work is isolated on `codex/notch-light-blackout`.

Brightness defaults to 0% with a 0–50% slider and exact original-level restoration. The previous brightness preference is preserved under its old key for rollback. Seated-to-departure transition no longer restores brightness before display sleep. A guarded private IOBluetooth compatibility reader supplements public connection events with stable per-bud wear changes; a known removal survives reconnect and unavailable metadata until stable reinsertion or explicit manual recovery. Reads run off the UI and motion-acquisition queues. Unsupported metadata retains the earlier public-event behavior; per-bud physical compatibility is not implied by a successful build.

Both AppModel and the capture owner ignore notifications for unchanged display layout. Verified sensor-acquisition continuity can preserve alignment across delayed UI delivery, while real gaps/source/clock changes invalidate it. Enable retries an existing camera reference when necessary, without choosing a new zero. These are concrete regression fixes; the supplied recordings were animation references and did not reproduce Activity Monitor, so they do not prove a single cause for that reported symptom.

The new notch grows downward into camera/curved-rail content, conditionally offers an explicit Face light card, then draws green loops and a custom smile before retracting. The success face remains visible during closing. Physical cutout exclusion, nonactivating focus, transparent hit regions and Reduce Motion remain covered. Face light is AirVeil-owned, starts off every check, and only appears after repeated close-face/low-face-light/poor-pose evidence. It stops on success, cancel, stale/lost face or inactivity. Apple's independent system Edge Light preference is not altered.

The semicircular blur-onset dial stores independent nonnegative left/right thresholds. Interaction clamps to the selected side and never wraps across center. Switching sides restarts the illustrative head from center; the full-obscuration threshold rises if needed to stay beyond both onsets. Native controls use the requested Apple-inspired design guidance.

The full core regression suite passed: 378 dial/onset, 2,094 motion/math, 62 delivery, 55 real MotionService, 60 fusion, 63 camera service, 109 centered coordinator, 48 guidance, 31 notch feedback, 255 AppModel/removal integration, 33 presence geometry, 14 presence capture, 106 brightness ownership/restoration, 46 removal coordinator, 22 per-bud evidence, 22 removal debounce, 24 display-sleep, and 690,785 input-geometry checks. Existing actual Metal rendering passed at 1x/2x. Hardware boundaries are injected in these policy tests; no wearer removal, camera, display-sleep or zero-backlight action is implied by them.

The wearer reserved physical acceptance for the end. The previous hardware brightness test covered 8%/10% and exact restoration, not the new 0% target. Either-bud removal and reinsertion, low-light camera presence after blackout, Activity Monitor during live tracking, perceived animation and illumination remain installed-app wearer checks. Future proposals are documented in [NEXT-FEATURES.md](../research/NEXT-FEATURES.md); none were added to this release.

Final capture-owner configuration tests pass 26 synthetic notification/running-state decisions. The native notch suite passes 18 screen-geometry and 35 lifecycle checks; 22 states rendered at 2x. The illustrative angle dial was inspected in the installed settings window: Left and Right independently changed from 11° to 26°, the correct arc/head preview followed, and both original 11.451893682065219° values were restored. The offscreen native bitmap harness documents a 3D-layer capture limitation and is not evidence of rendered head rotation; the installed-window inspection supersedes that artifact limitation.

The signed 0.11.0 build 14 is installed at `/Applications/AirVeil.app`. Its executable matches the build SHA-256 `a2d01b87c4b10978601f5e0cdb47864c3e27529f953bbd1984733ba2dc976303`; strict verification and the same certificate-bound designated requirement as build 13 pass. The previous app bundle remains in the ignored build directory. Fresh installed diagnostics confirm hidden settings at startup, existing Screen Recording access retained, automatic removal and seated dimming enabled, 0% target, Face light/camera off, no pending restore/assertion, and zero display-sleep requests. No motion is arriving in the inactive session, and per-bud metadata is unavailable in this state. Neither successful physical per-bud detection nor the reported Activity Monitor behavior is claimed verified by this startup snapshot.

## Independent-ear follow-up: diagnostic checkpoint, September 14

The wearer reported that nonstreaming-bud removal still failed. A concrete asynchronous polling bug was corrected: a still-fresh cached duplicate no longer clears confirmation evidence. All core suites pass again, including 26 per-bud evidence checks and 257 AppModel lifecycle checks. The latter now explicitly verifies that removal presence, seated blackout, and reinsertion restoration perform no screen-permission requests or capture starts. Native checks pass 18 geometry/35 lifecycle cases and 22 rendered states; the revised Enable blur label fits the controls card.

The UI now distinguishes blur enablement from automatic removal and explains that Screen Recording permission is only for live desktop blur. Per-ear status appears separately under the removal feature, with updates isolated from sensor-rate settings redraws. An explicit `--diagnose-airpods-wear` launch hook permits the production wear reader to run when the removal feature is disabled, without enabling that feature or changing its saved preference. It adds anonymous reader fields to the existing optional local JSON diagnostics.

A signed diagnostic build with these changes is installed (still version 0.11.0/build14, not a completed new release); installed and built executable SHA-256 match `645260f6a44a9fe24e01f07836f978d82b0be78d26ea4213e3e00c6c57b8d150`. The earlier build is retained locally. The user's current automatic-removal and camera-assistance preferences were off and remain off. Fresh synchronized observations show no motion and no connected headset, camera/capture off, no brightness changes and no display-sleep requests. This does not verify wear detection while connected. The new goal remains incomplete: synchronized both-worn/nonstreaming-removal/reinsertion data is needed, and the user's new “for dimming, both is necessary” policy clarification is pending. See [investigation](../research/INDEPENDENT-EAR-INVESTIGATION.md).

## Downloadable prerelease packaging, 0.11.1 (build 15)

The user explicitly requested publishing all current changes and a downloadable DMG while deferring hardware testing. No new AirPods behavior is claimed complete by this release. The existing code is packaged as a prerelease with version 0.11.1/build15; runtime defaults and unresolved per-ear acceptance remain unchanged.

`scripts/package-dmg.sh` rebuilds with the persistent development identity, stages only AirVeil.app, an Applications symlink and installation notes, then creates and verifies a compressed DMG and SHA-256 sidecar. A read-only mount verified exactly those three entries, the Applications destination, strict app signature, version/build, Apple silicon architecture, and byte-for-byte equality with the signed build. The volume was detached after inspection. DMG SHA-256: `3df19c014f40c8e2e7ae463b22225fa8d7f0eec7880ee6e507f4dae1ad010b27`. Packaged executable SHA-256: `24739c1870f25a5b5ce433859032b271a28b5e23d969615939c4270b74075943`.

Release notes identify the local development signature, lack of Apple notarization, private-repository access requirement, and pending physical verification. No security settings, repository visibility, local diagnostic data, or signing keys are changed or distributed by packaging. The existing running diagnostic app was not replaced by the packaging operation.
